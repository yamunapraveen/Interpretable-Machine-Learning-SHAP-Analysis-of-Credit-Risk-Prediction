# Interpretable-Machine-Learning-SHAP-Analysis-of-Credit-Risk-Prediction
To build and interpret a credit risk classification model that predicts loan default probability while ensuring model interpretability using SHAP (SHapley Additive exPlanations). You’ll analyze both global and local feature importance and translate model insights into business-level recommendations.
